<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AccountStatMapper">
    <resultMap id="accountStatResultMap" type="com.example.entity.AccountStat">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="loginCount" column="login_count"/>
        <result property="reportCount" column="report_count"/>
        <result property="lastLoginTime" column="last_login_time"/>
    </resultMap>

    <!-- 更新登录统计 -->
    <update id="updateLoginStat">
        UPDATE account_stats 
        SET login_count = login_count + 1,
            last_login_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 更新报告统计 -->
    <update id="updateReportStat">
        UPDATE account_stats 
        SET report_count = report_count + 1
        WHERE user_id = #{userId}
    </update>

    <!-- 获取用户统计信息 -->
    <select id="getUserStats" resultType="java.util.Map">
        SELECT 
            a.*,
            u.username,
            u.real_name
        FROM account_stats a
        LEFT JOIN user u ON a.user_id = u.id
        WHERE a.user_id = #{userId}
    </select>

    <!-- 获取所有用户统计信息 -->
    <select id="getAllUserStats" resultType="java.util.Map">
        SELECT 
            a.*,
            u.username,
            u.real_name
        FROM account_stats a
        LEFT JOIN user u ON a.user_id = u.id
        ORDER BY a.login_count DESC
    </select>
</mapper> 